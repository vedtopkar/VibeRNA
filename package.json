{
  "name": "ts-rna-draw",
  "version": "0.1.0",
  "description": "Experimental RNA structure drawing in typescript",
  "main": "index.js",
  "author": "Ved V. Topkar",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "bulma": "^0.9.1",
    "paper": "^0.12.11",
    "parcel-bundler": "^1.12.4",
    "structure": "^2.0.1",
    "typescript": "^4.1.3"
  },
  "scripts": {
    "build": "parcel build index.html --out-dir build --public-url /ts-rna-draw",
    "watch": "parcel index.html",
    "deploy": "yarn build && gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
